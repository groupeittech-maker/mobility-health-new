# Dockerfile prod : tout le code est copié dans l'image, pas de volume en runtime.
FROM python:3.11-slim

WORKDIR /app
ENV PYTHONPATH=/app

RUN mkdir -p /var/lib/mobility-health/uploads

RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    tesseract-ocr \
    tesseract-ocr-fra \
    poppler-utils \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
